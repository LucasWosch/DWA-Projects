// No topo do arquivo do componente (por exemplo, src/pages/index.tsx)

import React from 'react';
import KanbanBoard from './components/KanbanBoard'; // Ajuste o caminho se necessário

const HomePage = () => {
    return (
        <div>
            <KanbanBoard />
        </div>
    );
};

export default HomePage;
